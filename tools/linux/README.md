# Linux xdelta3 Tool

The Linux build expects a separately obtained executable at:

`tools/linux/xdelta3`

Make it executable with `chmod +x tools/linux/xdelta3`. xdelta3 is governed by its own license and is intentionally not included here.
